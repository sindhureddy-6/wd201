
    <!-- <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <li class="bg-red-600 text-white border border-red-500 p-1  rounded list-none w-fit"> <%= messages.error[i] %></li>
              <% } %>
    <% } %> -->
    <% if (messages.error) { %>
  <div class="bg-red-600 text-white border border-red-500 p-1  rounded list-none w-fit">
    <%= messages.error %>
  </div>
<% } %>
